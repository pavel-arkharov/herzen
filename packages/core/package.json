{
	"name": "@herzen/core",
	"version": "0.0.1",
	"type": "module",
	"main": "dist/index.js",
	"scripts": {
		"build": "tsc -p tsconfig.json",
		"dev": "pnpm --filter @herzen/dialog build && tsx src/index.ts",
		"dev:watch": "pnpm --filter @herzen/dialog build && tsx watch src/index.ts",
		"start": "pnpm --filter @herzen/dialog build && node --enable-source-maps dist/index.js",
		"lint": "eslint . --format json",
		"typecheck": "tsc -p tsconfig.json --noEmit",
		"test": "vitest run tests",
		"test:watch": "vitest tests",
		"clean": "rm -rf dist"
	},
	"dependencies": {
		"@herzen/audio": "workspace:*",
		"@herzen/dialog": "workspace:*",
		"@herzen/stt": "workspace:*",
		"@herzen/tts": "workspace:*",
		"@herzen/wakeword": "workspace:*"
	},
	"devDependencies": {}
}
